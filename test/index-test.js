const Index = require('../lib/index.js');

describe('Index', function(){

  it('player score should increase as it passes an obstacle', () => {
    const player = new Player("Jose");
    assert.isObject(player);
  });

})
